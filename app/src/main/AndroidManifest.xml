<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.runningtracker">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>  // POTREBNE DOZVOLE.
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> //  --
    
    <uses-permission android:name="android.hardware.location.gps"/>  // --
    <uses-permission android:name="android.hardware.location.network"/>  //--

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RunningTracker">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyDk0oI8T7yYy3hGc0SAL8AA6EWUziIv2HU"/>

    </application>

</manifest>